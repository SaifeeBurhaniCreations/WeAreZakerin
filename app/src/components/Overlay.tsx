import { StyleSheet, Text, View } from 'react-native'

const Overlay = () => {
  return (
    <View>
      <Text>Overlay</Text>
    </View>
  )
}

export default Overlay

const styles = StyleSheet.create({})